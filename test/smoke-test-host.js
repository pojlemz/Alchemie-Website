var host = require('../server/host');
describe('This just prints the value in the variable "host"', function() {
    it('Printing the value', function() {
        console.log("host: ", host);
    });
});